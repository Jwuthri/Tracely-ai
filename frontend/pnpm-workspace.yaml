# pnpm >=10 requires this file to declare `packages` even for a single-package repo;
# without it `pnpm install` fails with "packages field missing or empty".
packages:
  - .
allowBuilds:
  core-js: false
  esbuild: true
  sharp: true
